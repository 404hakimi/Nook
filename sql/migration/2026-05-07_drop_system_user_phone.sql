-- ============================================================
-- 迁移: 移除 system_user.phone 字段
-- 适用: 已经按旧 DDL 部署过、需要原地变更的环境
-- 全新部署的库无需执行(新版 sql/01_system.sql 已不含此列)
-- ============================================================

ALTER TABLE `system_user` DROP COLUMN `phone`;
