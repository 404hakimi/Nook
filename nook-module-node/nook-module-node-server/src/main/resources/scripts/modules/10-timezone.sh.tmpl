# Module 10 — 设置远端时区.

log "=== Module 10: 时区设置 → {{TIMEZONE}} ==="

if timedatectl set-timezone "{{TIMEZONE}}" 2>/dev/null; then
    log "✔ 时区已设为 {{TIMEZONE}}"
else
    warn "时区设置失败 (无效 IANA 名?): {{TIMEZONE}} — 跳过, 不阻塞"
fi
